// for bcrypt
export const SALT_ROUNDS = 10

// TODO(IL,2023-12-13): use i18next
export const i18n = {
  messages: {
    USERNAME_EXISTS: 'Username already exists',
    INVALID_USERNAME_OR_PASSWORD: 'Invalid username or password'
  }
} as const
